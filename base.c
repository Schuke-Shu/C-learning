#include <stdio.h> // 引入头文件

int main(int argc,char *argv[])
{
    int a = 1; // 声明变量
    printf("%d", a);
    int b = 0;
    printf("%d", b);

    return -1;
    // 单行注释
    /*
     * 多行注释
     * 多行注释
     */
    // 注释内容会被编译器忽略
}